Gallery database

This project's goal is to outline and put into use a gallery's database. Information on the paintings, events, and tickets is available to the user. This application allows the user to add paintings, schedule reports, and track the number of tickets that have been cut.


Instructions for installing & running the application:(windows)


-install python (python idle 3.9.1)
-install pip and sqlite3:
download get-pip.py: https://bootstrap.pypa.io/get-pip.py
download (download (py): download (zip) and run from the path of
<.../get-pip.py> py get-pip.py
modules:
py -m pip install sqlite3

-run the project.py code